version: "3.9"

services:
  app:
    build: .
    container_name: cars-management-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./database.db:/app/database.db
    restart: unless-stopped

